<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>
        Solidarity-Bond | {% block title %}
        {% endblock %}
    </title>
    <!--FontAwesome-->
    <script src="https://kit.fontawesome.com/8190148fda.js" crossorigin="anonymous"></script>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href={{ asset('css/bootstrap.min.css')}}>
    <!--Custom style sheet-->
    <link rel="stylesheet" href={{ asset('css/custom.css')}}>

    {% block stylesheets %}{% endblock %}
</head>

<body onload="checkCookie();">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark" id="navbar" style="background-color : #27251f;">
            <a class="navbar-brand" href="/">
                <div class="color-link">Solidarity-Bond</div>
            </a>

            <div class="dropdown d-lg-none ml-auto">
                <a class="nav-link" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    {% if app.user %} <div class="color-link">{{ app.user.firstname }}</div> {% else %} <div
                        class="color-link mr-5">Mon compte</div> {% endif %}
                </a>
                <div class="dropdown-menu dropdown-menu-right p-2 mw-250" arialabelledby="account-button">
                    <div class="row">
                        {% if not app.user %}
                        <div class="col-12">
                            <a href="{{path('security_register')}}" class="btn btn-dark color-link btn-block mt-1 mb-1">
                                S'inscrire
                            </a>
                        </div>
                        {% else %}
                        <div class="col-12">
                            <a href="{{path('security_account')}}" class="btn btn-dark color-link btn-block mt-1 mb-1">
                                Mon Profil
                            </a>
                        </div>
                        {% endif %}
                        {% if is_granted('ROLE_FABLAB')%}
                        <div class="col-12">
                            <a href="{{path('admin')}}" class="btn btn-dark color-link btn-block mt-1 mb-1">
                                Administration
                            </a>
                        </div>
                        {% else %}
                        {% endif %}
                        <div class="col-12">
                            <a href="{% if app.user %}  {{path('security_logout')}}    {% else %} {{path('security_login')}}     {% endif %}"
                                class="btn btn-dark color-link btn-block mt-1 mb-1">
                                {% if app.user %} Déconnexion {% else %} Connexion {% endif %}
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('index')}}"><div class="color-link">Accueil</div></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('show_products')}}"><div class="color-link">Boutique</div></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('about_group')}}"><div class="color-link">À Propos</div></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('contact')}}"><div class="color-link">Contact</div></a>
                    </li>
                </ul>
            </div>
            <div class="dropdown d-none d-lg-block ml-auto">
                <a class="nav-link" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    {% if app.user %} <div id="text_color">{{ app.user.FirstName }}</div> {% else %} <div
                        class="mr-5" id="text_color">Se connecter</div> {% endif %}
                </a>
                <div class="dropdown-menu dropdown-menu-right p-2 mw-250" arialabelledby="account-button">
                    <div class="row">
                        {% if app.user %}
                        <div class="col-12">
                            <a href="{{path('security_account')}}" class="btn btn-dark color-link btn-block mt-1 mb-1" id="footer">
                                Mon Profil
                            </a>
                        </div>
                        {% else %}
                        <div class="col-12">
                            <a href="{{path('security_register')}}" id="footer" class="btn btn-block mt-1 mb-1">
                                Inscription
                            </a>
                        </div>

                        {% endif %}
                    {% if is_granted('ROLE_FABLAB')%}
                    <div class="col-12">
                        <a href="{{path('admin')}}" class="btn btn-dark color-link btn-block mt-1 mb-1" id="footer">
                            Administration
                        </a>
                    </div>
                    {% else %}
                    {% endif %}
                        <div class="col-12">
                            <a href="{% if app.user %}{{path('security_logout')}}{% else %}{{path('security_login')}}{% endif %}"
                                class="btn btn-block mt-1 mb-1" id="footer" >
                                {% if app.user %} Déconnexion {% else %} Connexion {% endif %}
                            </a>
                        </div>
                    </div>
                </div>
            </div>

                <button class="btn" type="button">
                    <a href="{{ path('cart_index') }}"><i id="text_color" class="fa fa-shopping-cart mr-3 fa-1x" ></i></a>
                </button>
                
            </div>

        </div>
    </nav>



    </header>

    <main>
    {% block body %}
    {% endblock %}
    </main>
    
    <div id="cookiewarn" class=" fixed-bottom " style="display:none">
        <div class="" style="height:20%; width: 100%; text-align: center; position:relative; background-color:#d0d0ce
 ;">
            <div class="border" style="border-color : black">
            <p class="mt-3">Nous respectons votre vie privée. Nous utilisons des données non sensibles comme des cookies ou des identifiants électroniques pour conserver vos données de navigation entre vos visites et vous offir une meilleur expérience utilisateur. </p>
            <button class="btn btn-dark mb-3" onclick="HideCookieWarn();">Accepter</button>
            <button class="btn btn-dark mb-3" onclick="goPrev()">Refuser</button>
            </div>
        </div>
    </div>

    <script>
        function HideCookieWarn() {
            var x = document.getElementById("cookiewarn");
                if (x.style.display === "block") {
                        x.style.display = "none";  
                        isCookie = "true";
                        if (isCookie != "" && isCookie != null) {
                        setCookie("popup", isCookie, 365);
            }                      
                    } 
                    
            }
        function setCookie(cname, cvalue, exdays) {
        var d = new Date();
        d.setTime(d.getTime() + (exdays * 24 * 60 * 60 * 1000));
        var expires = "expires="+d.toUTCString();
        document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
        }
            
        function getCookie(cname) {
        var name = cname + "=";
        var ca = document.cookie.split(';');
        for(var i = 0; i < ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') {
        c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
        return c.substring(name.length, c.length);
        }
        }
        return "";
        }
        function checkCookie() {
        var isCookie = getCookie("popup");
        var x = document.getElementById("cookiewarn");
              
                        
        if (isCookie != "") {
            x.style.display = "none";     
        } else {
            x.style.display = "block";
        }
        
        }
        
        function goPrev()
        {
            window.history.back();
        }
    </script>

    

    <footer class="mt-3" id="footer">

        <div class="container-fluid">
            <div class="col-sm text-center mb-3 mt-2">
                            <h4>Suivez nous sur :</h4>
                            <a target="_blank"  id="text_color" href="https://www.facebook.com/pg/Solidarity-Bond-Strasbourg-102482274841635/posts/?ref=page_internal" ><i class="fab fa-facebook-f mr-3 fa-2x"></i></a>
                            <a target="_blank"  id="text_color" href="https://twitter.com/SolidarityStras" ><i class="fab fa-twitter mr-3 fa-2x"></i></a>
                            <a target="_blank"  id="text_color" href="https://www.instagram.com/solidarity_bond/" ><i class="fab fa-instagram mr-3 fa-2x"></i></a>
            </div>
        

        <div class="row text-center py-3" id="copyright">
            <div class="col-lg">
                <a href="{{path('about_fablab')}}" id="text_color">En savoir plus</a>
            </div>
            <div class="footer-copyright col-lg" >© 2020 Copyright:
                <a href="https://solidaritybond-stras.yj.fr/" id="text_color"> solidaritybond-stras.yj.fr</a>
            </div>
            <div class="col-lg">
                <a href="{{path('privacy_policy')}}" id="text_color">Politique de confidentialité</a>
            </div>
        </div>
        </div>
    </footer>


    
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
        integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
        crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.6/umd/popper.min.js"
        integrity="sha384-wHAiFfRlMFy6i5SRaxvfOCifBUQy1xHdJ/yoi7FRNXMRBu5WHdZYu1hA6ZOblgut"
        crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/js/bootstrap.min.js"
        integrity="sha384-B0UglyR+jN6CkvvICOB2joaf5I4l3gm9GU6Hc1og6Ls7i6U/mkkaduKaBhlAXv9k"
        crossorigin="anonymous"></script>
    <script src="{{asset('js/jQuery.js')}}"></script>
    {% block javascripts %}{% endblock %}
</body>

</html>