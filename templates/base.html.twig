<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>
        Solidarity-Bond | {% block title %}
        {% endblock %}
    </title>
    <!--FontAwesome-->
    <script src="https://kit.fontawesome.com/8190148fda.js" crossorigin="anonymous"></script>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href={{ asset('css/bootstrap.min.css')}}>
    <!--Custom style sheet-->
    <link rel="stylesheet" href={{ asset('css/custom.css')}}>

    {% block stylesheets %}{% endblock %}
</head>

<body onload="checkCookie();">
    <header>      
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="/">Solidarity-Bond</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample05" aria-controls="navbarsExample05" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarsExample05">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item ">
                    <a class="nav-link" href="{{path('index')}}">Accueil<span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="{{path('show_products')}}">Boutique</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="{{path('about_group')}}">À Propos</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="{{path('contact')}}">Contact</a>
                    </li class="nav-item">

                </ul>
                <ul class="navbar-nav navbar-right">
                    <li class="nav-item dropdown">
                        <div class="dropdown dropleft">
                            <button class="btn btn-dark dropdown-toggle nav-link" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                {% if app.user %}{{app.user.firstname}}{% else %}Mon compte{% endif %}                    
                            </button>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                {% if is_granted('ROLE_FABLAB')%}
                                <a class="dropdown-item btn btn-dark" href="{{path('admin')}}" id="btn"  >
                                    Administration FABLAB
                                </a>
                                {% endif %}
                                <a href="{% if app.user %}{{path('security_account')}}{% else %}{{path('security_register')}}{% endif %}" class="dropdown-item">{% if app.user %}Mon Profil{% else %}S'inscrire{% endif %}</a>
                                <a href="{% if app.user %}{{path('security_logout')}}{% else %}{{path('security_login')}}{% endif %}" class="dropdown-item">{% if app.user %}Déconnexion{% else %}Connexion{% endif %}</a>
                            </div>
                        </div>
                    </li>
                    <li class="nav-item" >
                    <a class="nav-link" href="{{ path('cart_index') }}" style="margin-left:10px"><i id="text_color" class="fa fa-shopping-cart fa-1x" ></i></a>
                    </li>     
                </ul>
            </div>
        </nav>
    </header>

    <main>
    {% block body %}
    {% endblock %}
    </main>
    
    <div id="cookiewarn" class=" fixed-bottom " style="display:none">
        <div class="" style="height:10%; width: 100%; text-align: center; position:relative; background-color:#d0d0ce;">
            <div class="border" style="border-color : black">
            <p class="mt-3">Nous respectons votre vie privée. Nous utilisons des données non sensibles comme des cookies ou des identifiants électroniques pour conserver vos données de navigation entre vos visites et vous offir une meilleur expérience utilisateur. </p>
            <button class="btn btn-dark mb-3" onclick="HideCookieWarn();">Fermer</button>
            </div>
        </div>
    </div>

    <script>
        function HideCookieWarn() {
            var x = document.getElementById("cookiewarn");
                if (x.style.display === "block") {
                        x.style.display = "none";  
                        isCookie = "true";
                        if (isCookie != "" && isCookie != null) {
                        setCookie("popup", isCookie, 365);
            }                      
                    } 
                    
            }
        function setCookie(cname, cvalue, exdays) {
        var d = new Date();
        d.setTime(d.getTime() + (exdays * 24 * 60 * 60 * 1000));
        var expires = "expires="+d.toUTCString();
        document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
        }
            
        function getCookie(cname) {
        var name = cname + "=";
        var ca = document.cookie.split(';');
        for(var i = 0; i < ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') {
        c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
        return c.substring(name.length, c.length);
        }
        }
        return "";
        }
        function checkCookie() {
        var isCookie = getCookie("popup");
        var x = document.getElementById("cookiewarn");
              
                        
        if (isCookie != "") {
            x.style.display = "none";     
        } else {
            x.style.display = "block";
        }
        
        }
        
        function goPrev()
        {
            window.history.back();
        }
    </script>

    

    <footer class="mt-3" id="footer">

        <div class="container-fluid">
            <div class="col-sm text-center mb-3 mt-2">
                            <h4>Suivez nous sur :</h4>
                            <a target="_blank"  id="text_color" href="https://www.facebook.com/pg/Solidarity-Bond-Strasbourg-102482274841635/posts/?ref=page_internal" ><i class="fab fa-facebook-f  fa-2x"></i></a>
                            <span style="margin-left : 20px;margin-right : 20px"></span>
                            <a target="_blank"  id="text_color" href="https://twitter.com/SolidarityStras" ><i class="fab fa-twitter fa-2x"></i></a>
                            <span style="margin-left : 20px;margin-right : 20px"></span>
                            <a target="_blank"  id="text_color" href="https://www.instagram.com/solidarity_bond/" ><i class="fab fa-instagram  fa-2x"></i></a>
            </div>
        

        <div class="row text-center py-3" id="copyright">
            <div class="col-lg">
                <a href="{{path('about_fablab')}}" id="text_color">En savoir plus</a>
            </div>
            <div class="footer-copyright col-lg" >© 2020 Copyright:
                <a href="https://solidaritybond-stras.yj.fr/" id="text_color"> solidaritybond-stras.yj.fr</a>
            </div>
            <div class="col-lg">
                <a href="{{path('privacy_policy')}}" id="text_color">Politique de confidentialité</a>
            </div>
        </div>
        </div>
    </footer>


    
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
        integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
        crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.6/umd/popper.min.js"
        integrity="sha384-wHAiFfRlMFy6i5SRaxvfOCifBUQy1xHdJ/yoi7FRNXMRBu5WHdZYu1hA6ZOblgut"
        crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/js/bootstrap.min.js"
        integrity="sha384-B0UglyR+jN6CkvvICOB2joaf5I4l3gm9GU6Hc1og6Ls7i6U/mkkaduKaBhlAXv9k"
        crossorigin="anonymous"></script>
    {% block javascripts %}{% endblock %}
</body>

</html>