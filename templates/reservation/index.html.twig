{% extends 'base.html.twig' %}

{% block title %}Hello ReservationController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% if is_granted('ROLE_FABLAB')%}
<form action="{{path('add_machine')}}" method="POST" >
    <input type="text" placeholder="Entrez le nom d'une machine" name="machine" id="name" required>
    <input type="submit" value="Mettre à jour">
</form>
{% endif %}


<div class="example-wrapper">
    <h1>Reservations</h1>
<br>
    Reserve:
    <ul class="list-group">
{% if machines is empty%}
<h5>no available machines<h5>
{% endif %}

{% for machine in machines %}
     <li class="list-group-item">
	<div class="dropdown">
  <a class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    {{machine.name}}
  </a>
  <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
    <a class="dropdown-item" href="machine">Morning</a>
    <a class="dropdown-item" href="machine">Afternoon</a>
  </div>
     </li>
{% endfor %}

</ul>
</div>
{% endblock %}
